# Tauri 桌面应用打包流水线：在 Linux 与 Windows 上构建并产出安装包
# （Windows 额外产出绿色免安装 zip）。
# - push 到 main / 提交 PR 时仅构建并上传产物；
# - 推送 v* tag 或手动触发时，额外创建 GitHub Release。
name: Package

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  package:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-22.04
            artifact: http-client-pro-linux-x86_64
          - platform: windows-latest
            artifact: http-client-pro-windows-x86_64
    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Tauri v2 Linux 构建所需的系统库（webkit2gtk / gtk / soup 等），
      # libclang-dev 供 rquickjs(bindgen) 生成 QuickJS 绑定使用。
      - name: Install Linux system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential curl wget file pkg-config \
            libwebkit2gtk-4.1-dev libjavascriptcoregtk-4.1-dev \
            libsoup-3.0-dev libgtk-3-dev librsvg2-dev \
            libxdo-dev libssl-dev libclang-dev

      # Windows 上为 bindgen 指定 libclang 位置（runner 已预装 LLVM）。
      - name: Set LIBCLANG_PATH (Windows)
        if: runner.os == 'Windows'
        run: echo "LIBCLANG_PATH=C:\Program Files\LLVM\bin" >> $env:GITHUB_ENV

      # 版本取自 package.json 的 packageManager 字段（pnpm@10.27.0）。
      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: Swatinem/rust-cache@v2

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      # 执行 pnpm --filter desktop tauri:build -> tauri build。
      # src-tauri 是仓库根 Cargo workspace 的成员，产物位于
      # 仓库根目录的 target/release/bundle/
      # （Linux: deb/rpm/appimage；Windows: msi/nsis + 绿色版 zip）。
      # 非 tag 构建不合并 tauri.release.conf.json（createUpdaterArtifacts
      # 保持关闭），因此不需要签名密钥，main / PR 构建不会因 secrets 缺失失败。
      - name: Build Tauri app
        if: "!startsWith(github.ref, 'refs/tags/v')"
        run: pnpm tauri:build

      # 仅打 v* tag 发版时启用更新包签名：额外合并
      # src-tauri/tauri.release.conf.json 打开 createUpdaterArtifacts，
      # 需要 TAURI_SIGNING_PRIVATE_KEY(_PASSWORD) secrets，
      # 产出 *.sig 供 release job 生成 latest.json。
      # 注意：TAURI_SIGNING_PRIVATE_KEY 的值是 .key 文件的原文两行
      # （含 "untrusted comment:" 行），不要 base64；私钥未设密码时
      # TAURI_SIGNING_PRIVATE_KEY_PASSWORD 无需配置，secrets 缺省展开为空串即可。
      - name: Build Tauri app (signed for release)
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        run: pnpm tauri:build:release

      # Windows 绿色免安装版：把 release 可执行文件（Tauri 单文件 exe，
      # WebView2Loader 已静态链接）直接压成 zip，解压即用；
      # 运行依赖系统的 WebView2 Runtime（Win10 21H2+ / Win11 默认已内置），
      # 用户数据仍写在 %APPDATA%\pro.http-client.desktop。
      # 产物放到 target/release/bundle/portable/ 以便随 bundle 一起上传。
      - name: Package Windows portable zip
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $config = Get-Content apps/desktop/src-tauri/tauri.conf.json -Raw | ConvertFrom-Json
          # 主二进制名：未配置 mainBinaryName 时为 crate 名，配置后为 productName，两者都试一遍。
          $candidates = @("$($config.productName).exe", 'http-client-pro-desktop.exe')
          $exe = $candidates |
            ForEach-Object { Join-Path 'target/release' $_ } |
            Where-Object { Test-Path $_ } |
            Select-Object -First 1
          if (-not $exe) { throw "release executable not found, tried: $($candidates -join ', ')" }

          $bundleDir = 'target/release/bundle/portable'
          $staging = Join-Path $bundleDir 'HTTP-Client-Pro'
          New-Item -ItemType Directory -Force -Path $staging | Out-Null
          Copy-Item $exe (Join-Path $staging "$($config.productName).exe") -Force

          $zip = Join-Path $bundleDir "HTTP-Client-Pro_$($config.version)_windows-x64-portable.zip"
          if (Test-Path $zip) { Remove-Item $zip -Force }
          Compress-Archive -Path $staging -DestinationPath $zip
          Remove-Item $staging -Recurse -Force
          Get-Item $zip | Select-Object Name, Length

      - name: Upload bundle artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: target/release/bundle/
          if-no-files-found: error

  release:
    needs: package
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      # checkout 必须排在下载产物之前：actions/checkout 默认 clean: true，
      # 会对工作区执行 git clean -ffdx，把未跟踪的 artifacts/ 与 latest.json
      # 一并删掉，导致 Release 只剩 GitHub 自动生成的 Source code 包
      # （v0.3.0 / v0.4.0 的安装包丢失就是这个原因）。
      # Release 说明取自带注释 tag 的正文（git tag -a 的 message），
      # 应用内「关于我们 → 更新日志」读取的正是 Release body，
      # 因此发版前请把本次更新内容写进 tag 注释。
      - name: Checkout (for tag annotation)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract release notes from tag
        env:
          TAG: ${{ github.ref_name }}
        run: |
          git tag -l --format='%(contents)' "$TAG" > release-notes.md || true
          if [ ! -s release-notes.md ]; then
            echo "HTTP Client Pro $TAG 发布。" > release-notes.md
          fi
          cat release-notes.md

      - name: Download bundle artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: http-client-pro-*
          path: artifacts

      # 生成 Tauri updater 静态更新清单 latest.json：
      # platforms.linux-x86_64 -> AppImage + .sig；windows-x86_64 -> NSIS setup.exe + .sig。
      - name: Generate latest.json
        env:
          TAG: ${{ github.ref_name }}
        run: |
          python3 - <<'EOF'
          import datetime
          import glob
          import json
          import os
          import re
          from urllib.parse import quote

          tag = os.environ["TAG"]
          base = f"https://github.com/Xtreme-Light/HTTP-Client-Pro/releases/download/{tag}"

          def find_one(pattern):
              matches = glob.glob(f"artifacts/**/{pattern}", recursive=True)
              return matches[0] if matches else None

          def asset_name(path):
              # GitHub 会把资产名中 [A-Za-z0-9._-] 之外的字符（如空格）替换成 "."，
              # 例如 "HTTP Client Pro_0.4.1_x64-setup.exe" 上传后实际下载地址是
              # "HTTP.Client.Pro_0.4.1_x64-setup.exe"。latest.json 必须写规范化后的
              # 名字，否则应用内「检查更新」下载 404。
              return re.sub(r"[^A-Za-z0-9._-]", ".", os.path.basename(path))

          platforms = {}
          for key, pattern in (
              ("linux-x86_64", "*.AppImage"),
              ("windows-x86_64", "*-setup.exe"),
          ):
              path = find_one(pattern)
              if not path:
                  print(f"warn: no bundle for {key} ({pattern})")
                  continue
              with open(path + ".sig", encoding="utf-8") as f:
                  signature = f.read().strip()
              platforms[key] = {
                  "signature": signature,
                  "url": f"{base}/{quote(asset_name(path))}",
              }

          data = {
              "version": tag.lstrip("v"),
              "notes": f"HTTP Client Pro {tag} 发布，详见 GitHub Release 说明。",
              "pub_date": datetime.datetime.now(datetime.timezone.utc).isoformat(),
              "platforms": platforms,
          }
          with open("latest.json", "w", encoding="utf-8") as f:
              json.dump(data, f, ensure_ascii=False, indent=2)
          print(json.dumps({k: v["url"] for k, v in platforms.items()}, indent=2))
          EOF

      # fail_on_unmatched_files：任一 glob 没匹配到文件就让 job 失败，
      # 避免再次出现「Release 建好了但一个安装包都没传上去」的静默事故。
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: HTTP Client Pro ${{ github.ref_name }}
          body_path: release-notes.md
          fail_on_unmatched_files: true
          files: |
            artifacts/**/*.deb
            artifacts/**/*.rpm
            artifacts/**/*.AppImage
            artifacts/**/*.msi
            artifacts/**/*.exe
            artifacts/**/*.zip
            artifacts/**/*.sig
            latest.json

  # ------------------------------------------------------------------
  # 可选：同步发布到 CNB 镜像（国内下载源）。
  # 前置条件（需仓库维护者提供）：
  #   1. 在 https://cnb.cool 注册账号（微信扫码）并创建组织与仓库，
  #      仓库路径需与代码中端点一致：Xtreme-Light/HTTP-Client-Pro
  #      （如不同，请同步修改 main.rs UPDATE_ENDPOINTS、tauri.conf.json、
  #        src/lib/updates.ts 中的 CNB 地址）；
  #   2. 在 GitHub Secrets 配置 CNB_TOKEN（CNB 个人访问令牌，
  #      需 repo 相关权限）；
  #   3. 将安装包与 latest.json 上传为 CNB Release 附件，
  #      且 tag 固定包含 "latest"（对应端点
  #      /-/releases/download/latest/latest.json）。
  # 具体上传方式（CNB OpenAPI / .cnb.yml attachments 插件）待 CNB
  # 仓库信息确认后补充。
  # ------------------------------------------------------------------
  # sync-cnb:
  #   needs: release
  #   if: startsWith(github.ref, 'refs/tags/v')
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Sync artifacts to CNB releases
  #       env:
  #         CNB_TOKEN: ${{ secrets.CNB_TOKEN }}
  #       run: echo "TODO: upload artifacts + latest.json to CNB release"
